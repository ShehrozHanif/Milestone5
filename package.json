{
  "name": "milestone5",
  "version": "1.0.0",
  "scripts": {
   "prebuild": "rm -rf node_modules && npm install",
   "build": "npm install -g typescript && npx tsc && cp index.html dist/index.html"


  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
